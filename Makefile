kilo: kilo.c
  $(CC) kilo.c -o kilo -Wall -Wextra -pedantic -std=c99

# $(CC) = standard
# kilo.c -o kilo compiles the app and generates the executable file*/
# -Wall = all Warnings*/
# -Wextra = Extra Warnings*/
# -pedantic = Even more messages*/
# -std=c99 -> C version we are using.*/
